# React Native + Expo's peer-dep declarations are notoriously over-strict.
# This flag matches what `npx expo install` uses internally, and stops
# editors (WebStorm, VS Code) from failing on `npm install` with ERESOLVE.
legacy-peer-deps=true
