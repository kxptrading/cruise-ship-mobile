# Copy to .env.local and fill in. These are pulled into the bundle at build time
# via expo-constants. The same Supabase project the web app uses.
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
